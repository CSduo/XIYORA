"I've completed a thorough security audit of the XIYORA backend. Here are ALL findings organized by severity, with specific file paths, line numbers, and code snippets.\n\n---\n\n## 🔴 CRITICAL FINDINGS (3)\n\n### C1. Admin Login: Plaintext Password Comparison — No Rate Limiting, No Brute-Force Protection\n**File:** `artifacts/api-server/src/routes/adminLogin.ts` Lines 7–21\n```typescript\nrouter.post(\"/admin/login\", async (req, res): Promise<void> => {\n  const { username, password } = req.body ?? {};\n  const expectedPassword = process.env.ADMIN_PASSWORD;\n  if (username !== expectedUsername || password !== expectedPassword) {\n    res.status(401).json({ error: \"Invalid credentials\" });\n    return;\n  }\n  const token = createAdminToken();\n  res.json({ success: true, token });\n});\n```\n**Issues:**\n- **No rate limiting** — unlimited brute-force login attempts allowed.\n- **Plaintext password comparison** using `===` — vulnerable to timing attacks. Should use `crypto.timingSafeEqual()`.\n- **No account lockout** after failed attempts.\n- **Default username is `\"admin\"`** (Line 9: `process.env.ADMIN_USERNAME || \"admin\"`), making brute-force easier.\n\n### C2. Admin Secret Header: Non-Timing-Safe Comparison — Authentication Bypass Risk\n**File:** `artifacts/api-server/src/lib/adminAuth.ts` Lines 15–19\n```typescript\nconst expected = process.env.ADMIN_SECRET;\nconst provided = req.header(\"x-admin-secret\");\nif (expected && provided && provided === expected) {\n  next();\n  return;\n}\n```\n**Issue:** The `x-admin-secret` header is compared with `===` (not timing-safe), making it vulnerable to timing side-channel attacks. Additionally, this means **ANY request with the raw secret in a header bypasses JWT entirely** — the secret doubles as both a JWT signing key AND a static API key, which is a design flaw.\n\n### C3. Hardcoded Admin Email in Google Login Route\n**File:** `artifacts/api-server/src/routes/adminLogin.ts` Line 32\n```typescript\nconst adminGoogleEmail = process.env.ADMIN_G
<truncated 10870 bytes>